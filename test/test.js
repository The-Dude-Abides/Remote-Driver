var expect = require("chai").expect;

// this is from the example, will need to update. Just adding files right now 
describe("Remote Driver", function() {
  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });

  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });

  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });

  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });

  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });

  it("should be self-consistent", function() {
    expect(2).to.equal(2);
  });
});
